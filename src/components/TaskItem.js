import {useState} from "react";import InputUpdate from "./InputUpdate";import styles from "./TaskListModule.css"const TaskItem = (props) => {    const [open, setOpen] = useState(false)    const toggle = () => {        setOpen(!open)    }    return (        <div className={props.task.done ? "done" : ''}> {props.task.title}            <div className="item-line">            <button id='btn'                onClick={() => props.changeStatus(props.task.id)}>{props.task.done === true ? "Undone" : "Done"}</button>                <span id='task-name'>{' '} {props.task.name}  {' '}</span>            <button id='btn'onClick={() => props.deleteTask(props.task.id)}>Delete</button>            <button id='btn' onClick={toggle}>Update</button>                {open && <InputUpdate task={props.task} updateTask={props.updateTask} toggle={toggle}/>}            </div>        </div>    );};export default TaskItem;